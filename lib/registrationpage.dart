import 'package:flutter/material.dart';

class Registrration extends StatelessWidget {
  const Registrration({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Screen 2')),
    );
  }
}
